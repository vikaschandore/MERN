//// Temporal dead zone

// since when this let is hoisted and it is allocated some
// value the time between these
// two actions is called temporal dead zone

//script

// stored in different memory than global they are also
// getting allocated memory but not attached to global object
// we cannot access these declartion before we provide some value to them

/// Refrence Error x is not define console.log(X)

/// Refrence error in case let cannot use it before initialization
